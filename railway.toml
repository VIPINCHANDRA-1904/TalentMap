[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python -c 'import nltk; nltk.download(\"punkt\"); nltk.download(\"stopwords\")' && python -m spacy download en_core_web_sm && python train_model.py"

[deploy]
startCommand = "gunicorn app:app"
healthCheckPath = "/"
